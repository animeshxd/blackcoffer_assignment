import 'package:flutter/material.dart';

final buttonBorderRadius = BorderRadius.circular(20);

const buttonPadding = EdgeInsets.all(0);
final ColorScheme mainColorScheme =
    ColorScheme.fromSeed(seedColor: Colors.pink);
final mainThemeData =
    ThemeData.from(colorScheme: mainColorScheme, useMaterial3: false);
